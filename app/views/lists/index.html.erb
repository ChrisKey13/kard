<h1>Welcome to your new world of learning</h1>
<p>We want to be the reference to help programmers learn the following languages:</p>
<ul>
  <% @languages.each do |language| %>
  <li>
    <%= language.name %>
  </li>
  <% end %>
</ul>
<ol>
  <% @lists.each do |list| %>
  <li>
      <%= link_to list.title, list %>
    <% end %>
  </li>
</ol>

<%= link_to 'Add A New List', new_list_path, class: 'btn btn-primary'  %>
